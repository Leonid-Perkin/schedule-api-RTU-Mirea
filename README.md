# Schedule API RTU MIREA

API для получения расписания РТУ МИРЭА. Проект использует Playwright для динамического скрапинга данных с официального сайта и предоставляет удобный интерфейс доступа к расписанию через FastAPI.

## ✨ Особенности

- 🚀 **FastAPI**: Высокая производительность и автоматическая документация Swagger/ReDoc.
- 🎭 **Playwright**: Надежное извлечение данных даже с динамических страниц.
- 📦 **Docker**: Готовая конфигурация для быстрого развертывания через Docker и Docker Compose.
- 💾 **Кэширование**: Сохранение результатов парсинга в локальные JSON-файлы для ускорения повторных запросов.
- 📅 **Гибкость**: Получение расписания на конкретный день или на всю неделю.

## 🛠 Технологии

- Python 3.x
- FastAPI
- Playwright (Chromium)
- Pydantic Settings
- Docker / Docker Compose

## 🚀 Быстрый старт

### Через Docker (рекомендуется)

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/your-username/schedule-api-RTU-Mirea.git
   cd schedule-api-RTU-Mirea
   ```

2. Запустите проект:
   ```bash
   docker-compose up -d --build
   ```

3. API будет доступно по адресу: `http://localhost:8000`

### Локальный запуск

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Установите браузеры Playwright:
   ```bash
   playwright install chromium
   ```

3. Запустите приложение:
   ```bash
   python main.py
   ```

## 📖 API Эндпоинты

После запуска документация доступна по адресам:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

### Основные запросы:

- **Расписание на день**:
  `GET /schedule?group=БАСО-03-24&date=2025-12-20`
  *(параметр `date` необязателен, по умолчанию — сегодня)*

- **Расписание на неделю**:
  `GET /schedule/week?group=БАСО-03-24&date=2025-12-20`
  *(возвращает расписание на всю неделю, к которой относится указанная дата)*

## ⚙️ Конфигурация

Настройки хранятся в `config.py`. Вы можете переопределить их через переменные окружения или файл `.env`:

- `HOST`: Хост (по умолчанию `0.0.0.0`)
- `PORT`: Порт (по умолчанию `8000`)
- `CACHE_TTL`: Время жизни кэша в секундах (по умолчанию 24 часа)
- `BROWSER_HEADLESS`: Запуск браузера в фоновом режиме (по умолчанию `True`)

## 📝 Лицензия

Проект распространяется под лицензией MIT. Подробности в файле [LICENSE](LICENSE).
